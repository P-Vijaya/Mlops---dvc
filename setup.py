from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description="its a admission prediction package",
    author="vijaya",
    packages=find_packages(),
    license="MIT"
)